
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        {{#categories}}

        {{#if childrens}}
        <li class="dropdown">
          <a href="/index.js?r=product/category&category_id={{_id}}"
          class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
          aria-expanded="false">{{name}}<span class="caret"></span></a>
          <ul class="dropdown-menu">
            {{#childrens}}
            <li><a href="/index.js?r=product/category&category_id={{_id}}">{{name}}</a></li>
            {{/childrens}}
            <li role="separator" class="divider"></li>
            <li>
              <a href="/index.js?r=product/category&category_id={{_id}}">See All</a>
            </li>
          </ul>
        </li>
        {{else}}
        <li>
          <a href="/index.js?r=product/category&category_id={{_id}}">{{name}}</a>
        </li>
        {{/if}}
        {{/categories}}
      </div>
    </div>
  </nav>
